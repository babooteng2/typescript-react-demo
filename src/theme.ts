import {DefaultTheme} from 'styled-components';

const myTheme:DefaultTheme = {
    /* borderRadius: "inherits", */
    colors: {
        /* main: "inherits",
        secondary: "inherits", */
        blueColor: "#2980b9",
    }    
}

export {myTheme}